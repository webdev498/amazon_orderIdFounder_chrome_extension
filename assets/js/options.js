$(document).ready(function() {
	// $("#hotkey").keyup(function(e) {
	// 	var keys = [],
	// 		curKey = String.fromCharCode(e.which);

	// 	if (e.ctrlKey)
	// 		keys.push("Ctrl");
	// 	else if (e.shiftKey)
	// 		keys.push("Shift");

	// 	if ( curKey && curKey !== "." ) {
	// 		keys.push(curKey);

	// 		$(this).val(keys.join(" + "));
	// 	}
	// })
})